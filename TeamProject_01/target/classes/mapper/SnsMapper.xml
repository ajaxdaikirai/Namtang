<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 게시판 처리 SQL -->
<mapper namespace="com.com.Project.DAO.SnsMapper">
	<insert id="writeSns" parameterType="Sns">
		insert into Sns(
			snsSeq,
    		userSeq,
    		snsContent         

		)values(
			SNS_SEQ.nextval,
			#{userSeq},
			#{snsContent}
		)
	</insert>
	
</mapper>